<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.dk.sample.api.app.search.mapper.SearchDAO">

    <select id="searchBySite" resultType="kr.co.dk.sample.api.common.util.LowerKeyMap">
        SELECT *
        FROM momo.work
        WHERE work_title LIKE CONCAT('%', #{value}, '%')
          AND work_status = 0
          AND work_type = 0
    </select>
<!--        SELECT site.*, company.company_logo-->
<!--        FROM momo.site site-->
<!--                 JOIN momo.company company USING (company_idx)-->
<!--        WHERE site_name LIKE CONCAT('%', #{value}, '%')-->
<!--          AND site_status = 0-->

    <select id="searchByCompany" resultType="kr.co.dk.sample.api.common.util.LowerKeyMap">
        SELECT *
        FROM momo.work
        WHERE work_constructor LIKE CONCAT('%', #{value}, '%')
          AND work_status = 0
          AND work_type = 0
    </select>

    <select id="searchByWork" resultType="kr.co.dk.sample.api.common.util.LowerKeyMap">
        SELECT *
        FROM momo.work
        WHERE work_title LIKE CONCAT('%', #{value}, '%')
          AND work_status = 0
          AND work_type = 0
    </select>

    <select id="searchByEquip" resultType="kr.co.dk.sample.api.common.util.LowerKeyMap">
        SELECT *
        FROM momo.construction_equipment
        WHERE construction_title LIKE CONCAT('%', #{value}, '%')
          AND construction_status = 0
    </select>

</mapper>
